{
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "config": "npm run lint && ./scripts/build_config.js",
    "sinusize": "npm run lint && ./scripts/build_sinusize.js"
  },
  "devDependencies": {
    "eslint": "^5.1.0",
    "js-yaml": "^3.12.0"
  }
}
